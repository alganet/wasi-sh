# build workspace (tarball, extracted tree, objects)
/build/work/

# build output — not committed; produced by `npm run build:wasm` or by CI,
# and packed into the npm tarball from a CI-built, smoke-tested artifact
/dist/

# node
node_modules/
package-lock.json

# OS
.DS_Store

# local-only investigation notes (never committed)
/BUSYBOX_TOOLS.md

# The wasm toolchain: `pip install ziglang` into a venv, per build/build.sh.
.venv/
